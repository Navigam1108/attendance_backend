# quickmark-auth-backend/.env

# Database URL (use your Render external DB URL here for both local and production)
# DATABASE_URL=your_render_external_db_url

# (Optional: legacy local config, not used if DATABASE_URL is set)
PGUSER=postgres
PGHOST=localhost
PGDATABASE=quickmark_db
PGPASSWORD=admin
PGPORT=5432

# JWT Secret (IMPORTANT: Change this to a strong, random string!)
JWT_SECRET=your_JWT_secret_here
JWT_EXPIRES_IN=7d

# Server Port
PORT=3700